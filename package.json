{
  "name": "bharat-heritage-root",
  "version": "1.0.0",
  "description": "Bharat Heritage Production Web & Backend Application for Render Deployment",
  "private": true,
  "main": "server/server.js",
  "scripts": {
    "build": "npm --prefix server install && npm --prefix server run build",
    "server": "npm --prefix server run server",
    "start": "npm --prefix server run start",
    "dev": "npm --prefix server run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
